[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "audit-chain"
version = "0.1.0"
description = "Per-tenant hash-chained audit log"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["audit_chain"]
